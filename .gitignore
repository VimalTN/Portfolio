# OS
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/

# working files not needed on the live site (optional — remove if you want them in the repo)
# spreads/
# *.pdf

# node (if you add tooling later)
node_modules/
.vercel

# agent skills / local tooling (installed via `npx skills add`) — not part of the site
.agents/
.claude/
skills-lock.json

# image backups
*_original.jpg
